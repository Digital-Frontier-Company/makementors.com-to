"use client"

/**
 * Central place for one-off client-side effects (e.g. scroll progress bar, GSAP registrations).
 * Right now this mounts nothing – extend as needed.
 */
export function AppEffects() {
  return null
}
